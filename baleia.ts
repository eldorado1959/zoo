class Baleia extends Mamifero {
    amamentar() {
      console.log(`${this.nome} está amamentando.`);
    }
    nadarProfundo() {
      console.log(`${this.nome} está nadando em águas profundas.`);
    }
    nadar() {
      console.log(`${this.nome} está nadando.`);
    }
  }
